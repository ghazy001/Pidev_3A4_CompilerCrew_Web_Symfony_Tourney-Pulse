<form method="post" action="{{ path('app_match_entity_delete', {'idMatch': match_entity.idMatch}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match_entity.idMatch) }}">
    <button class="btn">Delete</button>
</form>
