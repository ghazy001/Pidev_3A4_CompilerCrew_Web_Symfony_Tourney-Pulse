

 <div style="margin-top: 300px; margin-right: 300px; ">
{{ form_start(form) }}

    {{ form_widget(form) }}
    {{ form_errors(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
