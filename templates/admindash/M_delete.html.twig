<style>
    .custom-btn {
        background-color: #B0413E;
        color: #ffffff; /* Couleur du texte */
        border-color: #5d87ff; /* Couleur de la bordure */
    }
</style>
<form method="post" action="{{ path('mes_delete', {'id': message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>