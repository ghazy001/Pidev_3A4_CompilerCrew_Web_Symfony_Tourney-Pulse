<form method="post" action="{{ path('app_stade_delete', {'id': stade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stade.id) }}">
    <button class="btn">Delete</button>
</form>
