<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>Match Details</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #87A922;
            margin: 0;
            padding: 0;
            color: #FFAE1F;
        }

        .container {
            margin: 20px auto;
            width: 80%;
        }

        .match-details {
            background-color: #114232;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        .match-details h1 {
            text-align: center;
            color: #ffda6a;
            margin-bottom: 20px;
        }

        .match-details p {
            font-size: 40px;
            margin-bottom: 10px;
            line-height: 1.6;
        }

        .match-details p strong {
            font-weight: bold;
        }


    </style>
</head>
<body>
<div class="container">
    <div class="match-details">
        <h1>Match Details</h1>

        <p><strong>Tournament Name:</strong> {{ match_entity.nomMatch }}</p>

        <p><strong>Stadium Name:</strong> {{ match_entity.dateMatch ? match_entity.dateMatch|date('Y-m-d H:i:s') : '' }}</p>

        <p><strong>Number Of Matches:</strong> {{ match_entity.dureeMatch }}</p>

        <p><strong>Beginning Date:</strong> {{ match_entity.idTournois.nomTournois }}</p>

        <p><strong>End Date:</strong> {{ match_entity.idEquipe1.nom }}</p>

        <p><strong>End Date:</strong> {{ match_entity.idEquipe2.nom }}</p>
    </div>
</div>
</body>
</html>
